[package]
name    = "humanether_serde"
version = "0.1.0"
edition = "2024"

[dependencies]
alloy-primitives = { version = ">=1", features = ["serde"] }
serde            = { version = ">=1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
